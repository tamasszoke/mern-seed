'use strict'

import config, {
  settings,
  express,
  database,
  nodemailer,
  socket,
  stats,
  show,
  swagger,
} from './config'

export default config
export { settings, express, database, nodemailer, socket, stats, show, swagger }
